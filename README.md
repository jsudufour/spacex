### SpaceX API Page

This project uses the [SpaceX API](https://github.com/r-spacex/SpaceX-API/tree/master/docs/v4) to render collections of Rockets and Dragons.

#### Installation

`yarn install` to install dependencies

#### Running the app

`yarn start` to start the development server
